////EXTERNAL VARIABLES.
//Variables related to pin style.
const pinParams = {
  pinRadius: 4,                          //Saves the value of the radius of the pin.
  pinActiveBorderColor: "#949494",       //Saves the color of the border for an active pin.
  pinNonActiveBorderColor: "#949494",    //Saves the color of the border for a non active pin.
  pinActiveColor: "#c73c3c",             //Saves the color of an active pin.
  pinNonActiveColor: "#363636",          //Saves the color of a non active pin.
  pinTagColor: "#363636",                //Saves the color of the pin tag.
  pinTextFont: 'bold 10px Arial'         //Saves the font type of the text in the pin tag.
}

export { pinParams };