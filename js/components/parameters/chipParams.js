////EXTERNAL VARIABLES.
const chipParams = {
  chipPinsSpacing: 1,                    //Saves the spacing between pins.
  chipRadius: 5,                         //Saves the radius of the chip corners.
  chipBorderResolution: 3,               //Saves the number of vertices in the chip corners.
  chipCaseBorderColor: "#949494",        //Saves the border color of the chip case.
  chipCaseColor: "#949494",              //Saves the color of the chip case.
  chipTextColor: "#363636",              //Saves the color of the text in the chip case.
  chipTextFont: 'bold 10px Arial'        //Saves the font type of the text in the chip case.
}

export {chipParams};